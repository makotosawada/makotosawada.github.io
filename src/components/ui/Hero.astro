---
import { Image } from 'astro:assets'

import SocialIcons from './SocialIcons.astro'


const {fullName, title, institute, profilePicture} = Astro.props
---
<div class='flex-1'>
  <h1 class='text-4xl font-bold mb-4'>
    Welcome! I'm <span class='text-secondary'>{fullName}</span>
  </h1>
  <p class='text-xl text-gray-600 mb-4'>{title}{institute ? ` at ${institute}` : ''}</p>

  <div class='max-w-3xl'>
    <!-- 基盤となる指針 -->
    <p class='text-2xl font-bold text-gray-800 mb-6 tracking-tight'>
      &ldquo;Rigor, Insights, and Perseverance&rdquo;
    </p>

    <ul class='space-y-4 mb-8'>
      <li class='flex items-start'>
        <span class='text-secondary mr-3 text-xl'>•</span>
        <p class='text-lg leading-snug'>
          <span class='font-semibold'>Synthesizing complex mission components into scientific success</span> with critical interventions.
        </p>
      </li>
      <li class='flex items-start'>
        <span class='text-secondary mr-3 text-xl'>•</span>
        <p class='text-lg leading-snug text-gray-700 italic'>
          True astrophysics requires more than the worship of data; it demands the rigorous synthesis of the instruments and ideologies that produce it.
        </p>
      </li>
    </ul>
  </div>
  <div class='flex gap-4'>
    <SocialIcons />
  </div>
</div>
<div class='size-72 lg:block hidden'>
  <!-- Add your profile image -->
  <Image
    src={profilePicture}
    alt={fullName}
    class='w-full h-full mask mask-circle size-44'
  />
</div>
