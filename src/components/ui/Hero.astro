---
import { Image } from 'astro:assets'

import SocialIcons from './SocialIcons.astro'


const {fullName, title, institute, profilePicture} = Astro.props
---
<div class='flex-1'>
<div class='flex-1'>
  <h1 class='text-4xl font-bold mb-2'>
    {fullName}
  </h1>
  <p class='text-xl text-secondary font-medium mb-6'>
    {title}{institute ? ` @ ${institute}` : ''}
  </p>

  <div class='max-w-3xl'>
    <p class='text-2xl italic text-gray-800 mb-8'>
      &ldquo;Rigor, Insights, and Perseverance&rdquo;
    </p>
    
    <ul class='space-y-4 mb-8 text-lg text-gray-700 list-none'>
      <li class='flex items-start'>
        <span class='mr-3'>•</span>
        <span>Synthesizing complex mission components into scientific success with critical interventions.</span>
      </li>
      <li class='flex items-start'>
        <span class='mr-3'>•</span>
        <span>True astrophysics requires more than the worship of data; it demands the rigorous synthesis of the instruments and ideologies that produce it.</span>
      </li>
    </ul>
  </div>

  <div class='flex gap-4'>
    <SocialIcons />
  </div>
</div>

<div class='size-72 lg:block hidden'>
  <!-- Add your profile image -->
  <Image
    src={profilePicture}
    alt={fullName}
    class='w-full h-full mask mask-circle size-44'
  />
</div>
