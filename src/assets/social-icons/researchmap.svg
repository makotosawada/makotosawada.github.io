<svg xmlns="http://www.w3.org" width="640" height="640" viewBox="0 0 640 640">
  <g fill="none" stroke="currentColor" stroke-width="40" stroke-linecap="round" stroke-linejoin="round">
    
    <!-- Outer boundary (optional, if you want a box) -->
    <!-- <rect x="80" y="80" width="480" height="480" rx="60"></rect> -->

    <!-- Large Rounded Play Symbol (Triangle) -->
    <path d="M220 160 L460 320 L220 480 Z" fill="currentColor" stroke-width="0"/>

    <!-- Letters "rm" in the center -->
    <!-- Using a simpler font style for SVG compatibility -->
    <text x="260" y="370" font-family="sans-serif" font-size="120" font-weight="bold" fill="#FFF" stroke="none">rm</text>

  </g>
</svg>
